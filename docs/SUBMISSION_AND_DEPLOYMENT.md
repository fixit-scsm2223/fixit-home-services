# Submission and Deployment Notes

The project has been reorganized into the final submission layout:

```text
frontend/
backend/
database/
docs/
submission-checklist.md
README.md
```

Use these current documents:

- `README.md` for local setup and Android commands
- `docs/API_CONTRACT.md` for existing API endpoints
- `docs/DEPLOYMENT_GUIDE.md` for production and Capacitor deployment
- `submission-checklist.md` for final verification

The Android app must use the same deployed `backend/` API and the same `fixit_db` MySQL database. Do not create an Android-only API or database.
