{
  "name": "fixit-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:production": "vite build --mode production",
    "build:android": "vite build --mode android",
    "preview": "vite preview",
    "cap:sync:android": "vite build --mode android && cap sync android",
    "cap:open:android": "cap open android",
    "cap:run:android": "vite build --mode android && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@stripe/stripe-js": "^9.8.0",
    "axios": "^1.18.0",
    "leaflet": "^1.9.4",
    "pinia": "^3.0.4",
    "vue": "^3.5.34",
    "vue-router": "^5.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1",
    "@vitejs/plugin-vue": "^6.0.6",
    "vite": "^8.0.12"
  }
}
